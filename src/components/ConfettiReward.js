import React from 'react';
import { View, StyleSheet } from 'react-native';

export const ConfettiReward = ({ trigger, onComplete }) => {
  // Konfeti kütüphanesi olmadan basit versiyon
  return null;
};

const styles = StyleSheet.create({
  container: {
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    bottom: 0,
  },
});